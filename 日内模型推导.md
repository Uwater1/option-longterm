# 日内模型推导：从 Day-Model 到 Daytrade 的完整逻辑链

本文档从第一性原理出发，逐步推导日内交易系统的每一个设计决策。每一步都回答"为什么这样做"以及"不这样做会怎样"。

> **最新版本（2026-06-23）**：训练目标已从 `pm_return`（13:00→15:00 累积收益）切换为 `trade_return`（入场开盘→14:30 收盘），与回测 P&L 完全对齐。该变更使总部署 OOS Sharpe 从 +23.18 提升至 **+44.67**。

---

## 目录

1. [核心假设：下午收益可预测吗？](#1-核心假设下午收益可预测吗)
2. [特征工程：什么信息在上午可知？](#2-特征工程什么信息在上午可知)
3. [模型选择：为什么用线性模型？](#3-模型选择为什么用线性模型)
4. [特征筛选：130 个特征如何缩减到 3–70 个？](#4-特征筛选130-个特征如何缩减到-370-个)
5. [验证框架：如何确认信号不是噪声？](#5-验证框架如何确认信号不是噪声)
6. [从预测到交易信号：Frozen Score](#6-从预测到交易信号frozen-score)
7. [阈值设计：什么时候才值得交易？](#7-阈值设计什么时候才值得交易)
8. [回测引擎：信号如何变成收益？](#8-回测引擎信号如何变成收益)
9. [双模型演进：为什么 Single 不够？](#9-双模型演进为什么-single-不够)
10. [参数校准与部署：最终决策](#10-参数校准与部署最终决策)
11. [总结：完整数据流图](#11-总结完整数据流图)

---

## 1. 核心假设：下午收益可预测吗？

### 1.1 预测目标

定义 **Trade 收益** 为从决策后入场到 14:30 出场的对数收益：

$$r_{\text{trade}} = \log\left(\frac{P_{\text{exit}}^{\text{close}}}{P_{\text{entry}}^{\text{open}}}\right) = \log\left(\frac{P_{\text{EXIT\_BAR}}^{\text{close}}}{P_{\text{decision\_bar}+1}^{\text{open}}}\right)$$

其中 `decision_bar` 是每只 ETF 的信号触发 bar（收盘触发决策），`EXIT_BAR = 41`（14:30 收盘）。入场发生在 `decision_bar + 1` 的开盘价——这是决策后第一笔可执行的交易。

**为什么从 pm_return 切换到 trade_return？** 旧目标 `pm_return`（13:00–15:00 累积收益）与实际交易窗口不匹配：模型在上午 9:45–10:00 决策，但预测的是下午 13:00 才开始的收益，中间 3 小时的信息被忽略。`trade_return` 精确覆盖了「决策 bar 收盘 → 入场 bar 开盘 → 14:30 收盘」的完整交易区间，消除了目标-策略错配。

实测：切换目标后，总部署 OOS Sharpe 从 +23.18 翻倍至 +44.67。所有 10 个 ETF×方向组合均可部署（旧目标仅 5/10）。

### 1.2 假设的可检验性

零假设 $H_0$：$r_{\text{trade}}$ 不可被上午信息预测（即有效市场）。

备择假设 $H_1$：开盘前 15–30 分钟的量价行为 + 前一日技术面，包含对交易收益的微弱预测力。

**检验指标**：Spearman 秩相关系数（IC, Information Coefficient），衡量预测排名与实际排名的相关性：
- IC = 0 → 无预测力
- |IC| > 0.03 → 金融信号中"微弱但可能可交易"
- |IC| > 0.10 → 强信号

实测结果（Holdout，即样本外，single 模型 vs `trade_return`）：

| ETF | Holdout IC | L/S Sharpe | 模型类型 | 选中特征数 |
|-----|-----------|------------|---------|----------|
| 300ETF | +0.021 | +1.20 | Huber | 3 |
| 50ETF | +0.033 | +1.68 | Huber | 12 |
| 500ETF | +0.128 | +2.49 | ElasticNet | 39 |
| 588000ETF | -0.015 | +1.17 | Ridge | 21 |
| 159915ETF | +0.101 | +2.77 | ElasticNet | 41 |

结论：500ETF、159915ETF 信号最强（IC > 0.10，L/S Sharpe > 2.0）。300ETF、50ETF 边际可用。588000ETF 单模型 IC 为负，但通过双模型（Long/Short 独立训练）仍可部署。

**注意**：以上为 single 模型诊断。实际部署使用双模型（Long/Short 独立训练 + 混合模式部署），总 OOS Sharpe 达 +44.67（详见 §9）。

---

## 2. 特征工程：什么信息在上午可知？

### 2.1 时序因果约束

**铁律**：所有特征必须在决策时刻（10:00 或更早）之前可计算。任何需要"未来"数据的特征都是 look-ahead bias（前视偏差），会导致回测虚高、实盘崩溃。

### 2.2 三类特征（共 130 个）

| 类别 | 数量 | 信息来源 | 可计算时刻 |
|------|------|----------|-----------|
| **Early-bar（早盘）** | 50 | 前 6 根 5 分钟 bar（9:30–10:00） | 10:00 |
| **Day-level（日线）** | 58 | 技术指标 + 第三方数据，全部 shift(1) | 前一日收盘 |
| **Yesterday（昨日）** | 22 | 前一日的早盘 + 全天特征 | 前一日收盘 |

### 2.3 关键设计细节

**Early-bar 特征**：
- 每根 bar 的收益率 `bar_ret_i`、成交量 `bar_vol_i`、振幅 `bar_rng_i`
- 前 30 分钟总体特征：`early_realized_vol`（已实现波动率）、`early_skew`（偏度）、`early_kurtosis`（峰度）
- VWAP 偏离度 `bar_vwap_dev_i`：当前价格与成交量加权均价的距离

**Day-level 特征**（严格 shift(1)）：
- 技术指标：`macd_hist`, `rsi5`, `roc10`, `bb_pctb`, `atr14_norm` 等
- 第三方数据：融资融券 `margin_net_buy`、北向资金 `northbound_net`、VIX 相关 `vix_iv_spread`
- **VIX/IV 价差的特殊处理**：`vix_iv_spread = vix - iv`。某些 ETF 的 VIX 数据起步较晚（如 300ETF 从 2019 年），缺失值用 `iv + 历史均值偏差` 回填

**Yesterday 特征**：
- `yesterday_pm_return`、`yesterday_am_return`：前一日的下午/上午收益
- `yesterday_day_vwap_dev`：前一日收盘相对 VWAP 的位置
- `yesterday_early_skew`：前一日早盘偏度

### 2.4 成交量特征的无偏归一化

如果直接用当日总成交量归一化早盘 bar 成交量，就会引入前视偏差（需要收盘后才知道全天量）。解决方案：

$$\text{expected\_bar\_vol} = \frac{\text{过去 20 日滚动日均成交量（shift 1）}}{48}$$

这保证了归一化因子仅依赖历史信息。

---

## 3. 模型选择：为什么用线性模型？

### 3.1 信噪比决定模型复杂度

`trade_return` 的信噪比极低（IC 通常 0.02–0.13）。在这种环境下：
- **复杂模型**（神经网络、随机森林）→ 容易过拟合噪声
- **线性模型** → 参数少、可解释、对噪声更鲁棒

### 3.2 模型族选择

| 模型 | 正则化 | 对异常值鲁棒性 | 适用场景 |
|------|--------|--------------|---------|
| Ridge | L2 | 否（MSE 敏感） | 基线对照 |
| Lasso | L1 | 否 | 特征选择 |
| ElasticNet | L1+L2 | 否 | 特征共线时 |
| **HuberRegressor** | L2 | **是**（$\epsilon$ 控制） | **首选** |

**Huber 损失函数**：

$$L_\epsilon(r, \hat{r}) = \begin{cases} \frac{1}{2}(r - \hat{r})^2 & |r - \hat{r}| \leq \epsilon \\ \epsilon |r - \hat{r}| - \frac{1}{2}\epsilon^2 & |r - \hat{r}| > \epsilon \end{cases}$$

当 $\epsilon = 1.35$（默认值附近），对超过 $\sim 1.35\sigma$ 的残差从二次惩罚变为线性惩罚。这对金融数据至关重要——单日暴涨暴跌（如 2015 年股灾、2020 年疫情）不会主导模型参数。

实测：5 个 ETF 中 single 模型有 2 个选择了 Huber，2 个选择了 ElasticNet，1 个选择了 Ridge。双模型训练下模型类型更加多样。

---

## 4. 特征筛选：130 个特征如何缩减到 3–70 个？

### 4.1 为什么不能直接用全部 130 个特征？

- 样本量 ~2700 天，特征 130 个 → $p/n \approx 0.05$。虽不是"维度灾难"，但大量弱特征会引入噪声
- 很多特征高度相关（如 `bar_ret_0` 和 `first_bar_return` 几乎是同一个东西）
- 过拟合风险随特征数线性增长

### 4.2 Block Bootstrap Stability Selection

**核心思想**：一个好特征应该在不同的训练子集上都被选中。如果一个特征只在某些特定数据切片上被选中，那它可能只是在拟合噪声。

**算法步骤**：

1. 将训练数据分成 20 天的 block（保持时序相关性）
2. 有放回地抽取 50 次 bootstrap 样本
3. 每次用 `LassoCV` 做特征选择
4. 计算每个特征的**稳定性分数** = 被选中的频率（0%–100%）
5. 设定阈值（如 50%）→ 高于阈值的特征保留

**为什么用 block 而不是普通 bootstrap？** 金融时间序列有自相关性。普通 bootstrap 打乱时序会破坏相关性结构，导致过于乐观的稳定性估计。20 天 block 保留了短期的自相关模式。

### 4.3 阈值作为超参数

稳定性阈值不是固定的，而是通过 Optuna 在 $[0.40, 0.90]$ 范围内搜索。Optuna 的目标函数是 **Walk-Forward CV 的平均 Spearman IC**。

| ETF | 最优阈值 | 选中特征数 | 解读 |
|-----|---------|-----------|------|
| 300ETF | 0.90 | 3 | 极稀疏（信号弱，只保留最强的） |
| 50ETF | 0.70 | 12 | 中等稀疏 |
| 500ETF | 0.60 | 39 | 密集（信号强，多特征有贡献） |
| 588000ETF | 0.65 | 21 | 中等（IC 为负，特征不稳定） |
| 159915ETF | 0.70 | 41 | 密集且信号极强 |

---

## 5. 验证框架：如何确认信号不是噪声？

### 5.1 Purged Walk-Forward Validation

**问题**：`trade_return` 覆盖决策 bar+1 → EXIT_BAR 的收益，训练集和测试集之间可能有泄漏（相邻日期的特征可能有时序相关性）。

**解决方案**：在训练集和测试集之间插入 5 天的 **purge gap**（清洗间隔），确保训练样本的 forward target 不会泄漏到测试集。

```
|←──── Train ────→|← gap →|←──── Test ────→|
                   5 days
```

5 折 expanding window：每折训练集增大，测试集向前滚动。

### 5.2 过拟合诊断

**IS-OOS Gap**（样本内 vs 样本外的 IC 差距）：

| ETF | IS IC | OOS IC | Gap | 评估 |
|-----|-------|--------|-----|------|
| 300ETF | 0.070 | 0.021 | 0.050 | 低 |
| 50ETF | 0.139 | 0.033 | 0.106 | 中等 |
| 500ETF | 0.193 | 0.128 | 0.065 | 低 |
| 588000ETF | 0.220 | -0.015 | 0.235 | 较高（单模型信号弱） |
| 159915ETF | 0.243 | 0.101 | 0.142 | 中等 |

Gap < 0.15 说明过拟合程度可控（588000ETF 除外，但其单模型信号本身很弱，依赖双模型部署）。

**Purge-Gap 敏感性测试**：如果 IC 随 purge gap 增大而急剧下降，说明存在短期泄漏。实测所有 ETF 在 gap=0→10 时 IC 变化 < 0.02，无泄漏。

### 5.3 年度稳定性

逐年 OOS IC 检查信号是否在不同市场环境下稳定：
- 159915ETF：大部分年份 IC > 0.05，最稳定
- 500ETF：大部分年份 IC > 0.05，个别年份略降
- 300ETF：各年波动较大，但方向基本一致
- 588000ETF：2024 年 strong，2025–2026 年有所衰减
- 50ETF：Short 方向稳定，Long 方向较弱

---

## 6. 从预测到交易信号：Frozen Score

### 6.1 核心概念

训练完成后，模型系数被"冻结"为常数。运行时**不需要任何 ML 库**，只需做一次线性运算：

$$\text{score}_t = \beta_0 + \sum_{i=1}^{k} \beta_i \cdot \tilde{x}_{i,t}$$

其中 $\tilde{x}_{i,t}$ 是经过 StandardScaler 标准化后的特征值。

### 6.2 Score 的含义

- **符号**（正/负）→ 方向预测：正 = 预测 trade_return 为正（做多有利），负 = 预测 trade_return 为负（做空有利）
- **绝对值**（大小）→ 置信度：值越大，模型越确信

这是一个**有符号的连续值**，不是概率。它近似预测的 trade_return 排名（因为 Spearman IC > 0 意味着高 score 倾向于对应高实际收益）。

### 6.3 为什么"冻结"？

- **无运行时依赖**：部署只需 `joblib.load()` + 一次矩阵乘法
- **防止过拟合漂移**：系数不随新数据变化，信号特性稳定
- **可审计**：每天可以精确复现 score 的计算过程
- **代价**：不能自适应新市场环境，需要定期重训

---

## 7. 阈值设计：什么时候才值得交易？

### 7.1 问题：不是每天都值得交易

Score 每天都有，但大部分日子 score 很小（方向不确定）。只在高置信度时交易才能盈利。

### 7.2 为什么用 Expanding Percentile 而不是固定阈值？

**固定阈值的问题**：score 的分布随时间变化（市场波动率变化、模型特征漂移）。一个在 2020 年有效的固定阈值可能在 2025 年过松或过紧。

**Expanding Percentile 的方案**：

$$\text{threshold}_t = Q_q\left(\{|\text{score}_s| : s < t\}\right)$$

即当前阈值 = 过去所有 |score| 的 $q$ 分位数。这自动适应分布变化。

**关键约束**：只使用 $t$ 之前的历史 → 无前视偏差。使用 `.shift(1)` 确保。

### 7.3 条件阈值：Long 和 Short 必须分开

**核心洞察**：score 的正值和负值有不同的分布。

- 当 score > 0 时（Long 候选），|score| 的分布可能有特定的均值和方差
- 当 score < 0 时（Short 候选），|score| 的分布可能完全不同

如果用对称阈值（所有 |score| 一起做 percentile），Long 和 Short 的选择标准会被对方的分布"稀释"。

**解决方案**：`expanding_pct_masked`——
- Long 阈值只在历史 score > 0 的日子上计算
- Short 阈值只在历史 score < 0 的日子上计算

这使得每边的阈值都是该边"条件分布"的分位数，选择更精确。

### 7.4 信号触发条件

**Single Mode（默认）**：

```
Long fires  ⟺  score > 0  AND  |score| ≥ long_threshold  AND  |score| ≥ long_conviction
Short fires ⟺  score < 0  AND  |score| ≥ short_threshold AND  |score| ≥ short_conviction
```

Long 和 Short **互斥**（score 不可能同时为正和负），无需冲突解决。

**参数**：
- `threshold_pct ∈ {50, 60, 70, 80, 90, 95}`：历史分位数阈值
- `conviction_pct ∈ {40, 50, 60, 70, 80, 90}`：额外置信度地板

---

## 8. 回测引擎：信号如何变成收益？

### 8.1 每日执行流程

```
09:30 开盘
  │
  │  收集前 N 根 5 分钟 bar（N 因 ETF 而异）
  │    300ETF  → bar 3（09:50 决策）
  │    50ETF   → bar 2（09:45 决策）
  │    500ETF  → bar 4（09:55 决策）
  │    588000ETF → bar 2（09:45 决策）
  │    159915ETF → bar 4（09:55 决策）
  ▼
决策 bar 收盘（close[DECISION_BAR]）
  │
  │  计算 score → 判断是否触发
  │  如果触发：direction = +1（Long）或 -1（Short）
  ▼
入场 @ open[DECISION_BAR + 1]（决策后第一根 bar 的开盘价）
  │
  │  持有（v1 无日内管理）
  ▼
出场 @ 14:30 收盘价（close[bar 41]）
```

**为什么入场在 next-bar open 而不是 decision-bar close？** 决策依赖 `close[DECISION_BAR]`，该价格在 bar 收盘时才知道。现实中不可能在同一根 bar 的收盘价同时完成计算和下单。`open[DECISION_BAR+1]` 是决策后第一笔可执行的成交，也是 `trade_return` 目标的入场价——模型训练与回测完全一致。

### 8.2 收益计算

$$r_{\text{gross}} = d \times \left(\frac{P_{\text{exit}}}{P_{\text{entry}}} - 1\right) = d \times \left(\frac{P_{41}^{\text{close}}}{P_{\text{decision\_bar}+1}^{\text{open}}} - 1\right)$$

$$r_{\text{net}} = r_{\text{gross}} - \frac{c}{10000}$$

其中 $d \in \{+1, -1\}$ 为方向，$c = 15$ bps 为单边交易成本。这与训练目标 `trade_return` 完全一致（训练用对数收益，回测用简单收益，差异极小）。

### 8.3 为什么选择 14:30 出场？

- 14:30 比 15:00 流动性更好（避免尾盘集合竞价的不确定性）
- 实测部分 ETF 在 14:30–15:00 有均值回复倾向，持有到 15:00 不一定更好

### 8.4 为什么不同 ETF 的决策时间不同？

Day-Model 的 §7（Bar Count 实验）发现每只 ETF 的最优决策 bar 不同：

| ETF | DECISION_BAR | 决策时刻 | 原因 |
|-----|-------------|---------|------|
| 300ETF | 3 | 9:50 | 蓝筹股需要 ~20 分钟"消化"才能形成预测力 |
| 50ETF | 2 | 9:45 | 大盘蓝筹，开盘交叉信息已足够 |
| 500ETF | 4 | 9:55 | 中盘股，更多 bar 提供更丰富的量价信号 |
| 588000ETF | 2 | 9:45 | 科创板，开盘即形成较强方向性 |
| 159915ETF | 4 | 9:55 | 成长股，需要更多 bar 捕捉趋势 |

更早决策 = 更多时间执行 = 更好的填充率。但需要信号在那个时刻已经成熟。每只 ETF 的 `DECISION_BAR` 控制了多少根 early-bar 特征被使用（超出部分填 0.0）。

---

## 9. 双模型演进：为什么 Single 不够？

### 9.1 Single Mode 的局限

Single mode 用一个**对称**模型预测 raw `trade_return`，然后 sign 决定方向。这隐含假设：

> 预测上涨的特征和预测下跌的特征是**同一组**，只是符号相反。

现实中不一定如此。例如：
- 高波动率可能同时预示大涨和大跌（不确定性增加）
- 某些技术形态可能只在一侧有效

### 9.2 双模型的 v1 失败教训

最初的方案是训练两个"不对称"模型（目标为 `pm_return`）：
- Long 模型：目标 $y_{\text{long}} = \max(0, r_{\text{PM}})$（截断）
- Short 模型：目标 $y_{\text{short}} = \max(0, -r_{\text{PM}})$（截断）

**三个致命问题**：

1. **截断目标违反线性回归假设**：~50% 的标签恰好为 0（零膨胀分布），OLS/Huber 的最优解是"到处预测接近 0"
2. **阈值稀释**：双模型的 score 分布偏正（均值 > 0），使得 90 分位数的门槛比 single mode 低 → 选入更多低质量交易
3. **Hybrid 是假双系统**：方向仍由 single model 的 sign 决定，dual model 只是过滤器

### 9.3 v2 的正确修复（trade_return 训练标准）

v2 在切换 `trade_return` 目标的同时，重新设计了双模型训练流程。核心思路：**两个模型都在 raw `trade_return` 上训练（保持完整回归信号），但通过不对称的特征筛选和优化目标让每边专注于自己的交易区间。**

#### 9.3.1 训练目标：Raw trade_return（非截断）

Long 和 Short 模型都使用 raw `trade_return` 做训练：

$$y_{\text{train}} = r_{\text{trade}} = \log\left(\frac{P_{\text{EXIT\_BAR}}^{\text{close}}}{P_{\text{decision\_bar}+1}^{\text{open}}}\right)$$

这避免了 v1 的核心问题——截断目标破坏了线性回归假设。模型看到完整的正负收益分布，系数不被零膨胀扭曲。

#### 9.3.2 稳定性选择目标：截断目标（仅用于特征筛选）

截断目标 **仅** 用于 Block Bootstrap Stability Selection 的特征筛选阶段：

$$y_{\text{clip}}^{\text{long}} = \max(0, r_{\text{trade}}), \quad y_{\text{clip}}^{\text{short}} = \max(0, -r_{\text{trade}})$$

Long 模型的稳定性选择在 $y_{\text{clip}}^{\text{long}}$ 上运行 LassoCV → 选出的特征是「上行驱动因子」，不是整体方差解释因子。Short 模型同理，选出「下行驱动因子」。Single 模型仍使用 raw $y$ 做稳定性选择。

#### 9.3.3 样本权重：温和强调活跃区间（$\lambda = 0.5$）

$$w_i = 1.0 + 0.5 \times \frac{\max(0, \pm y_i)}{\sigma}$$

其中 $\sigma = \text{std}(y_{\text{raw}})$，$+$ 用于 Long，$-$ 用于 Short。一个 $+2\sigma$ 的日收益仅获得 ~2× 权重，大部分日子保持 ~1×。这避免了 v1 Experiment C 中 $\lambda=2.0$ 导致的过拟合问题。

#### 9.3.4 尾部加权 IC 优化目标

Optuna 的目标函数对双模型加入尾部 IC 权重：

$$\text{Objective} = (1 - w_{\text{tail}}) \times \text{IC}_{\text{overall}} + w_{\text{tail}} \times \text{IC}_{\text{top-30\%}}$$

- Single 模型：$w_{\text{tail}} = 0.0$（纯全局 IC）
- Dual 模型：$w_{\text{tail}} = 0.5$（50% 全局 + 50% 顶部 30% 尾部 IC）

我们只在 score 的 top quantile 交易，所以优化目标应该侧重尾部排名质量。

#### 9.3.5 Rank 归一化与独立执行（Phase 1 + 3）

**Phase 1：Rank 归一化（修复阈值稀释）**

将 raw score 转换为 expanding percentile rank：

$$\text{rank}_t = \frac{|\{s < t : \text{score}_s < \text{score}_t\}|}{|\{s < t : \text{score}_s \text{ is valid}\}|}$$

输出 $\in [0, 1]$，不受原始分布形状影响。阈值 `thr=0.9` 严格选择历史 top 10%。

**Phase 3：真正独立的执行（修复假双系统）**

```
Long fires  ⟺  long_rank ≥ long_thr   （无需 single model sign gate）
Short fires ⟺  short_rank ≥ short_thr
Both fire?  → margin resolve: 哪边 margin 更高选哪边
```

#### 9.3.6 Hybrid 模式

Hybrid 模式结合了 single 的方向判断和 dual 的尾部信心：
- 方向由 single model sign 决定
- Conviction = |single_score| × dual_side_score（rank-normalized）
- 在 300ETF 和 500ETF 上表现突出（见 §9.4）

**Phase 4：混合模式部署**

每个 ETF × 每边，从 single/hybrid/dual 中选择 OOS Sharpe 最高的模式（`deploy.py`）。

### 9.4 v2 结果（trade_return 训练标准）

| ETF | Side | Single S | Hybrid S | Dual S | **部署模式** |
|-----|------|----------|----------|--------|------------|
| 50ETF | long | — | — | **+1.44** | **dual** |
| 50ETF | short | **+9.80** | +2.47 | +2.75 | **single** |
| 300ETF | long | +0.61 | **+3.63** | +1.29 | **hybrid** |
| 300ETF | short | +0.80 | **+2.76** | +2.04 | **hybrid** |
| 500ETF | long | +2.97 | **+5.57** | +4.17 | **hybrid** |
| 500ETF | short | +1.88 | **+4.25** | +1.63 | **hybrid** |
| 588000ETF | long | **+5.75** | +2.81 | +2.49 | **single** |
| 588000ETF | short | **+3.48** | +2.48 | +2.50 | **single** |
| 159915ETF | long | **+3.07** | +2.44 | +1.08 | **single** |
| 159915ETF | short | **+4.92** | +4.67 | +1.74 | **single** |

**总部署 OOS Sharpe**：**+44.67**（vs single-only +33.29，$\Delta = +11.38$；vs 旧 pm_return 基线 +23.18，$\Delta = +21.49$）

关键收获：
- **所有 10 个 ETF×方向组合均可部署**（旧目标仅 5/10，pm_return v2 仅 6/10）
- 300ETF 两边通过 hybrid mode 从不可部署变为可用（Long +0.61→+3.63, Short +0.80→+2.76）
- 500ETF 两边通过 hybrid mode 大幅提升（Long +2.97→+5.57, Short +1.88→+4.25）
- 50ETF Long 通过 dual mode **新上线**（single mode 无法触发，+1.44）
- Hybrid 模式在 trade_return 下成为主导模式（4/10 部署选择 hybrid）
- Dual mode 补充了 Single 覆盖不到的边角（50ETF Long）

---

## 10. 参数校准与部署：最终决策

### 10.1 校准网格

每个 ETF × 每边独立搜索：
- `threshold_pct ∈ {50, 60, 70, 80, 90, 95}`
- `conviction_pct ∈ {40, 50, 60, 70, 80, 90}`

共 $6 \times 6 = 36$ 种组合 × 2 边 × 5 ETF = 360 次回测，约 3 分钟完成。

### 10.2 选择目标

**Profit-first 复合分数**（与 Put 策略一致的评分体系）：

| 成分 | 权重 | 含义 |
|------|------|------|
| P&L | 35% | 累计净收益 |
| FilterLift | 30% | 选择性（交易占比越低越好 → 精准度） |
| Sharpe | 15% | 风险调整收益 |
| MaxDD | 10% | 最大回撤 |
| WinRate | 5% | 胜率 |
| Placement | 5% | 交易保留率（$1 - \text{FilterLift}$） |

### 10.3 硬性资格门槛

一个配置必须满足：
- OOS P&L > 0（样本外必须盈利）
- OOS Sharpe > 0（风险调整收益为正）
- OOS 交易次数 $n \geq 20$（统计显著性）

不满足的边直接标记为 `disabled`。

### 10.4 选择窗口

校准使用 **Holdout**（2024-03-19 之后），与 Day-Model 的 OOS 窗口完全对齐。这确保：
- Day-Model 声称的 OOS IC → Daytrade 声称的 OOS Sharpe，是同一段时间
- 不存在"Day-Model 用 OOS 选了好的特征，Daytrade 又在另一段 OOS 上验证"的双重 dipping

---

## 11. 总结：完整数据流图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         Day-Model（训练阶段）                            │
│                                                                          │
│  5m K线 + 日线 + 第三方数据                                              │
│       │                                                                  │
│       ▼                                                                  │
│  build_features.py ──▶ 130 个特征（50 Early-bar + 58 Day-level           │
│       │                   + 22 Yesterday）                               │
│       │  目标: trade_return = log(close[41] / open[decision_bar+1])      │
│       ▼                                                                  │
│  Block Bootstrap Stability Selection（50 次 × 20-day block）              │
│       │  Long/Short 模型使用截断目标做特征筛选（Phase 2.4）               │
│       ▼                                                                  │
│  Optuna Walk-Forward CV（100 trials, Purged TimeSeriesSplit）             │
│       │  Dual 模型: 50% 全局 IC + 50% top-30% 尾部 IC（Phase 2.5）       │
│       │  样本权重: 1.0 + 0.5 × max(0,±y)/σ                              │
│       ▼                                                                  │
│  最优模型（Huber/ElasticNet/Ridge）+ 选中特征（3–41 个）                  │
│       │  Single: 1 个模型/ETF    Dual: 2 个模型/ETF (long + short)       │
│       ▼                                                                  │
│  linear_{ETF}[_{side}].joblib + scaler_{ETF}[_{side}].joblib             │
└──────────────────────────────┬───────────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      Daytrade（推理 + 交易阶段）                          │
│                                                                          │
│  scores.py: score_t = intercept + Σ coef_i × standardized_feature_i     │
│       │  Single: 1 个 signed score    Dual: long_score + short_score     │
│       ▼                                                                  │
│  rules.py:                                                              │
│    ① Single: sign of score → direction                                  │
│    ② Hybrid: single direction × dual conviction (rank-normalized)        │
│    ③ Dual: independent rank → per-side threshold → margin resolve        │
│    ④ 条件 expanding percentile threshold（per-side, walk-forward）        │
│       │                                                                  │
│       ▼                                                                  │
│  calibrate.py: 网格搜索 (threshold, conviction) → 最优配置               │
│       │                                                                  │
│       ▼                                                                  │
│  deploy.py: 每个 ETF × 每边，选 single/hybrid/dual 中 OOS 最优的模式     │
│       │                                                                  │
│       ▼                                                                  │
│  backtest.py:                                                            │
│    entry = open[decision_bar + 1]  （决策后首根 bar 开盘）                │
│    exit  = close[bar 41]           （14:30 收盘）                        │
│    net_ret = direction × (exit/entry - 1) - cost                         │
│       │                                                                  │
│       ▼                                                                  │
│  REPORT.md: 总部署 OOS Sharpe +44.67（10/10 可部署）                     │
└──────────────────────────────────────────────────────────────────────────┘
```

### 关键设计原则回顾

| 原则 | 实现 | 违反的后果 |
|------|------|----------|
| 目标-策略对齐 | `trade_return` = 入场开盘→出场收盘 | 训练与回测错配，Sharpe 减半 |
| 无前视偏差 | 所有特征 shift(1) 或仅用早盘数据 | 回测虚高，实盘崩溃 |
| 过拟合控制 | Stability selection + Purged CV + Holdout | 选中噪声特征 |
| 鲁棒估计 | Huber loss + Block bootstrap | 异常日主导模型 |
| 不对称训练 | Raw 训练 + 截断稳定性 + 样本权重 + 尾部 IC | 对称模型遗漏单边信号 |
| 条件阈值 | Per-side expanding percentile masked | Long/Short 互相稀释 |
| 混合部署 | Per-side 最优模式选择（single/hybrid/dual） | 丢失互补信号 |
| 冻结系数 | 运行时无 ML 拟合 | 不能自适应，需定期重训 |
| 独立验证 | Day-Model OOS 窗口 = Daytrade OOS 窗口 | 防止双重 dipping |
